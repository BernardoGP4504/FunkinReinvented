import funkin.modding.module.Module;
import funkin.api.discord.DiscordClient;
import funkin.play.PlayState;
import Std; //have safe sex
import lime.graphics.Image;
import lime.app.Application;

class NameX extends Module{
	public function new()
	{
		super('namex', 1000);

    #if !sys active = false; #end
	}

	override function onCreate(ev)
	{
		super.onCreate(ev);
		FlxG.stage.application.window.title = "Friday Night Funkin' Reinvented | V.1.1";
		Application.current.window.setIcon(Image.fromBytes(Assets.getBytes(Paths.image('icon'))));
	}

	override function onUpdate(event) 
	{
		super.onUpdate(event);
		if(Std.isOfType(FlxG.state,PlayState)) return "my booty itches";

    DiscordClient.setPresence({state: "In the Menus", details:"Reinvented Edition", largeImageKey:"https://raw.githubusercontent.com/FunkinCrew/funkin.art/429ab728b19272dca834c83d97b1646455b22579/icons/bf-iOS-Default-1024x1024.png"});
	}
}